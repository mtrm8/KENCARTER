# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# local deploy config can hold plaintext API keys — never commit it
wrangler.jsonc

# macOS junk + dependencies
.DS_Store
node_modules/
